# Godot cache — never commit these
.godot/
*.import
*.uid

# Exported binaries — tracked as zips in builds/ only
builds/*.exe
builds/*.pck

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# VS Code
.vscode/

# JetBrains
.idea/
